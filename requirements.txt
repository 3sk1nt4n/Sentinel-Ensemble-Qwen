pydantic==2.13.4
mcp==1.28.0
defusedxml==0.7.1      # hardened XML parsing of untrusted evidence (EVTX, scheduled tasks, RDP)
pycryptodome==3.23.0   # crypto helpers used by the coordinator
rich>=13.0             # console UI (src/sift_sentinel/console.py, proc_cleanup) - needed at import/collection
psutil>=5.9            # process cleanup + host resource checks (proc_cleanup, onboarding)
python-evtx>=0.8       # Windows Event Log (EVTX) parsing for disk_extended / RDP artifacts
pytest==9.0.3          # kept installable out of the box: judges run the verify commands in JUDGE-QUICKSTART §7
